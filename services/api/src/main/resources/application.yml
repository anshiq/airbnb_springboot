spring:
  application:
    name: rental-platform-api
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/mydb}
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:admin123}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        format_sql: true
#        default_schema: public
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: anshikthind01@gmail.com
    password: "fplqgpbjsylmayby"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: ${SERVER_PORT:8085}
  servlet:
    context-path: /api/v1

app:
  jwt:
    secret: ${JWT_SECRET:AnshikRentalPlatformSecretKeyForJwtHS2562026}
    access-token-expiry-ms: ${JWT_ACCESS_EXPIRY:90000000}       # 1500 minutes
    refresh-token-expiry-ms: ${JWT_REFRESH_EXPIRY:604800000}  # 7 days
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174}
  frontend:
    customer-url: ${FRONTEND_CUSTOMER_URL:http://localhost:5173}
    admin-url: ${FRONTEND_ADMIN_URL:http://localhost:5174}
  razorpay:
    key-id: ${RAZORPAY_KEY_ID:rzp_test_T1RpU3qWD9u91s}
    key-secret: ${RAZORPAY_KEY_SECRET:2jPiRTvZ1gBFTHuHJsiAnZJT}
    webhook-secret: ${RAZORPAY_WEBHOOK_SECRET:placeholder_webhook_secret}
  platform:
    service-fee-percent: ${SERVICE_FEE_PERCENT:12.0}
    tax-percent: ${TAX_PERCENT:8.0}
    payout-delay-days: ${PAYOUT_DELAY_DAYS:7}
  seed: true
  super-admin:
    email: admin@rentalplatform.com
    password: ChangeMe123!
    first-name: Super
    last-name: Admin

springdoc:
  api-docs:
    path: /docs
  swagger-ui:
    path: /docs/ui
    operationsSorter: method
    tagsSorter: alpha
    try-it-out-enabled: true
  default-produces-media-type: application/json

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    com.rental: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
